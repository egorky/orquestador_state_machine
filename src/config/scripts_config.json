{
    "scripts": [
        {
            "name": "format_specialities_list",
            "function_body": "return specialities.map(s => s.speciality_name).join(', ');"
        },
        {
            "name": "check_if_user_is_minor",
            "function_body": "const birth_year = parseInt(id_number.substring(0, 4)); const current_year = new Date().getFullYear(); return (current_year - birth_year) < 18;"
        }
    ]
}
